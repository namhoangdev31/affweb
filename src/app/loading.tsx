import { LoadingScreen } from "@/components/loading-screen";

export default function GlobalLoading() {
  return <LoadingScreen title="Đang tải dữ liệu..." subtitle="Đang kết nối hệ thống Hoàn Tiền" />;
}

import { LoadingScreen } from "@/components/loading-screen";

export default function ShopLoading() {
  return (
    <LoadingScreen title="Đang tải Cổng Kênh KOC..." subtitle="Đang đồng bộ dữ liệu giao diện" />
  );
}
